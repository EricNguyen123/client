export enum Weekday {
  MONDAY = 0,
  TUESDAY = 1,
  WEDNESDAY = 2,
  THURSDAY = 3,
  FRIDAY = 4,
  SATURDAY = 5,
  SuUNDAY = 6,
}

export enum Role {
  ADMIN = 0,
  CUSTOM = 1,
  AUTHOR = 2,
}

export enum RoleEnum {
  ADMIN = 'admin',
  CUSTOM = 'user',
  AUTHOR = 'author',
}

export enum Status {
  Active = 1,
  Inactive = 0,
}

export enum StatusEnum {
  Active = 'active',
  Inactive = 'inactive',
}

export enum ErrorNumber {
  NOT_FOUND = 401,
}
